apiVersion: v1
kind: Secret
metadata:
  name: wss-secret
data:
  token: {{ .Values.secret.token }}
  face-subscription: {{ .Values.secret.face.subscription }}
  face-endpoint: {{ .Values.secret.face.endpoint }}
type: Opaque
